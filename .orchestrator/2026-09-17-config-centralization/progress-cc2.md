# CC2 进度（GuiConfig 三字段扩展与装配消费）

- 2026-09-17 20:26 CP1 落盘：契约三字段（rdRequireApproval/rdFps/tunnelServeAllow）双镜像（gui/cli types.rs）+ serde 字段级缺省 + Default 补齐；config.rs 测试拆出 config/tests.rs（300 行红线）并扩展红绿双向断言；types roundtrip json 抽 sample_json 复用；5 个集成测试 fixture 补齐穷举字面量；gui-contract §3 三字段入表。证据：cargo check --lib 双包 0 错；cargo test --lib（gui）157 passed；cargo test --bin p2pctl types 7 passed（含新增 config_rd_tunnel_fields_default_and_roundtrip）。
- 2026-09-17 20:31 CP2 落盘：rd 装配消费。rd/defaults.rs 新子模块（rd.rs 超 300 行红线）——审批闸 None 回落配置/Some 覆盖双分支单测；rdFps 越界回落 15 并 warn；rd_host_install 传 cfg.rd_fps 进 HostConfig.fps。rd-host 修复隐性断链：HostConfig.fps 此前被 `..HostState::default()` 吞掉（仅泵回退才读），with_config 现以 initial_quality 接线为初始档位（影响面：CLI rd host --fps 由死参数变为真实生效；p2p-itest 全部用缺省 15，零行为变化）。证据：cargo test -p rd-host --lib 1 passed；cargo test --lib rd:: 7 passed。
- 2026-09-17 20:35 CP3 落盘：tunnel serve 白名单写通持久化。tunnel_serve_start 受理成功后去重合并写 config.tunnelServeAllow（ConfigStore 原子保存，失败 warn 不静默）；node_start 装配 seed 持久化白名单进 gate（非法条目丢弃告警、开关仍关）。证据：cargo test --lib tunnel:: 12 passed（新增 persist 去重重启恢复 / 失败路径 / seed 恢复三测）。
- 2026-09-17 20:40 CP4 落盘：rd.rs 纯结构瘦身。parse_peer/random_session_id/default_fs_root/NoopSink 零行为迁移 rd/util.rs（命令定义不动，§19 路径字面保持）；rd.rs 342→305（任务接手时 334，净降 29）。查证 scripts/check/line-limit.sh 只扫 crates/，apps 侧为纪律线；crates/rd-host/src/lib.rs 282 行合规。
