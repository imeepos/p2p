# 工具链锁定：本地与 CI 强制同版，杜绝 rustfmt/clippy 版本漂移导致的
# 「本地绿 CI 红」类假象（2026-09-06 门禁修复轮的 ubuntu 定因依赖
# 「本地==CI 同为 1.98.1」这一前提，前提漂移排查成本即整轮 CI 债务）。
# 升级方式：版本号随 stable 前进手动 bump，随门禁提交一起评审。
[toolchain]
channel = "1.98.1"
