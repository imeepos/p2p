[package]
name = "a2a"
version.workspace = true
edition.workspace = true
description = "A2A over P2P 纯库：AgentCard/签名信封/AgentBook/帧编解码/task 状态机（docs/design/a2a-over-p2p-design.md §4/§5）"

# 零网络零进程：只承载模型/帧/簿/状态机纯逻辑，宿主接线在 apps/acp-agent
[dependencies]
bs58.workspace = true
p2p-identity = { path = "../p2p-identity" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror.workspace = true
tracing.workspace = true
